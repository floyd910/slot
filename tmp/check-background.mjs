const tabs=await(await fetch('http://localhost:9338/json/list')).json();const tab=tabs.find(t=>t.url.includes('localhost:5174'));if(!tab)throw Error('No local tab');const ws=new WebSocket(tab.webSocketDebuggerUrl);await new Promise(r=>ws.addEventListener('open',r,{once:true}));let n=0;const pending=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id){pending.get(m.id)?.(m.result);pending.delete(m.id);}};const send=(method,params={})=>new Promise(r=>{pending.set(++n,r);ws.send(JSON.stringify({id:n,method,params}));});
console.log(JSON.stringify(await send('Runtime.evaluate',{expression:`JSON.stringify({url:location.href,background:[...document.querySelectorAll('.game_area__background,.game_area > .bg-overlay')].map(e=>({cls:e.className,loaded:e.complete,width:e.naturalWidth,rect:e.getBoundingClientRect().toJSON(),bg:getComputedStyle(e).background,opacity:getComputedStyle(e).opacity,display:getComputedStyle(e).display})),text:document.body.innerText.slice(0,160)})`,returnByValue:true})));ws.close();
