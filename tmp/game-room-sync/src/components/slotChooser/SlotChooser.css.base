.slot-chooser {
  position: relative;
  isolation: isolate;
  display: grid;
  place-items: center;
  width: 100%;
  height: 100vh;
  height: 100svh;
  height: 100dvh;
  max-height: 100vh;
  max-height: 100svh;
  max-height: 100dvh;
  min-height: 0;
  padding: clamp(18px, 5vmin, 50px) clamp(18px, 7vw, 100px);
  overflow: hidden;
  background-image: var(--slot-chooser-bg);
  background-position: center center;
  background-repeat: no-repeat;
  background-size: cover;
}

.slot-chooser,
.slot-chooser * {
  -webkit-tap-highlight-color: transparent;
  -webkit-touch-callout: none;
  -webkit-user-drag: none;
  -webkit-user-select: none;
  -moz-user-select: none;
  -ms-user-select: none;
  user-select: none;
}

.bg-overlay {
  position: absolute;
  inset: 0;
  z-index: 0;
  background: #000;
  opacity: 0.4;
  pointer-events: none;
}

.slots {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  align-content: center;
  align-items: center;
  justify-content: center;
  justify-items: center;
  width: min(100%, 1180px);
  max-height: 100%;
  gap: clamp(18px, 4vmin, 64px);
}

.slot {
  width: 100%;
  max-width: 240px;
  min-width: 0;
  padding: 0;
  border: 0;
  background: transparent;
  appearance: none;
  -webkit-appearance: none;
  cursor: pointer;
  line-height: 0;
  touch-action: manipulation;
}

.slot:disabled {
  cursor: default;
  opacity: 1;
}

.slot img {
  display: block;
  width: 100%;
  height: auto;
  pointer-events: none;
}

@media (max-width: 1129px) {
  .slot-chooser {
    padding: clamp(16px, 5vmin, 44px) clamp(18px, 6vw, 50px);
  }

  .slots {
    grid-template-columns: repeat(3, minmax(0, 1fr));
    width: min(100%, 860px);
    gap: clamp(14px, 3.4vmin, 32px);
  }

  .slot {
    max-width: 220px;
  }
}

@media (max-width: 760px) {
  .slot-chooser {
    padding: clamp(12px, 3.4dvh, 24px) clamp(14px, 6vw, 32px);
  }

  .slots {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    width: min(100%, 430px);
    gap: clamp(8px, 2.2dvh, 16px) clamp(10px, 4vw, 18px);
  }

  .slot {
    max-width: 176px;
  }
}

@media (max-width: 380px) {
  .slot-chooser {
    padding: 10px 12px;
  }

  .slots {
    width: min(100%, 340px);
    gap: 8px 10px;
  }

  .slot {
    max-width: 152px;
  }
}

@media (max-width: 760px) and (max-height: 620px) {
  .slot-chooser {
    padding: 10px 14px;
  }

  .slots {
    width: min(100%, 360px);
    gap: 6px 10px;
  }

  .slot {
    max-width: 142px;
  }
}

@media (orientation: landscape) and (max-height: 520px) {
  .slot-chooser {
    padding: 10px 24px;
  }

  .slots {
    grid-template-columns: repeat(4, minmax(0, 1fr));
    width: min(100%, 760px);
    gap: 10px 16px;
  }

  .slot {
    max-width: 150px;
  }
}

.slot-chooser__continue {
  position: absolute;
  top: clamp(14px, 3vh, 32px);
  left: 50%;
  z-index: 2;
  display: flex;
  align-items: center;
  gap: 14px;
  max-width: calc(100% - 28px);
  padding: 10px 12px 10px 16px;
  border: 1px solid rgba(225, 191, 100, 0.72);
  border-radius: 10px;
  background: rgba(13, 13, 13, 0.9);
  color: #fff;
  font-family: Roboto, sans-serif;
  font-size: 14px;
  line-height: 1.3;
  box-shadow: 0 8px 22px rgba(0, 0, 0, 0.38);
  transform: translateX(-50%);
}

.slot-chooser__continue button {
  min-height: 36px;
  padding: 0 14px;
  border: 1px solid #e1bf64;
  border-radius: 7px;
  background: linear-gradient(145deg, #e1bf64, #aa7c11);
  color: #15110a;
  font: inherit;
  font-weight: 700;
  white-space: nowrap;
  cursor: pointer;
}

.slot-chooser__continue button:disabled {
  cursor: default;
  opacity: 0.55;
}

.slot {
  position: relative;
}

.slot--active {
  filter: drop-shadow(0 0 10px rgba(225, 191, 100, 0.7));
}

.slot__active-badge {
  position: absolute;
  right: 8px;
  bottom: 8px;
  display: inline-flex;
  min-height: 28px;
  align-items: center;
  padding: 0 9px;
  border: 1px solid rgba(255, 239, 185, 0.8);
  border-radius: 999px;
  background: rgba(13, 13, 13, 0.9);
  color: #f3d77e;
  font-family: Roboto, sans-serif;
  font-size: 11px;
  font-weight: 700;
  line-height: 1;
  pointer-events: none;
}

@media (max-width: 760px) {
  .slot-chooser__continue {
    top: 8px;
    gap: 8px;
    padding: 7px 8px 7px 11px;
    font-size: 12px;
  }

  .slot-chooser__continue button {
    min-height: 32px;
    padding-inline: 10px;
  }

  .slot__active-badge {
    right: 4px;
    bottom: 4px;
    min-height: 24px;
    padding-inline: 7px;
    font-size: 9px;
  }
}